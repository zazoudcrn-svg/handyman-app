<div class="py-5" style="font-family: 'Manrope', sans-serif; background-color: #F8FAFC; min-height: 100vh;">
  <div class="container" style="max-width: 1200px;">

    <%# HEADER %>
    <div class="mb-5 pb-4 border-bottom" style="border-color: #E2E8F0 !important;">
      <h1 class="h2 mb-0" style="font-family: 'Outfit', sans-serif; color: #0B1B33;">
        <%= current_user.customer? ? "Offers Received" : "My Offers" %>
      </h1>
    </div>

    <%# FILTER FORM %>
    <%= form_with url: offers_path, method: :get, local: true do |f| %>
      <div class="d-flex align-items-center flex-wrap gap-3 mb-4">
        <span class="text-uppercase fw-bold" style="font-size: 10px; color: #94A3B8; letter-spacing: 0.05em;">Filter by:</span>

        <%# LISTING — customer only %>
        <% if current_user.customer? %>
          <div class="dropdown">
            <button class="btn custom-filter-btn <%= params[:listing_id].present? ? 'active' : '' %>"
                    type="button" data-bs-toggle="dropdown" data-bs-auto-close="outside">
              Listing
              <i class="bi bi-chevron-down ms-1" style="font-size: 10px;"></i>
            </button>
            <div class="dropdown-menu p-3 shadow-sm" style="min-width: 320px; border-radius: 12px; border: 1px solid #E2E8F0;">
              <% Listing.where(id: @offers.pluck(:listing_id).uniq).each do |listing| %>
                <div class="form-check mb-2">
                  <%= radio_button_tag :listing_id,
                      listing.id,
                      params[:listing_id] == listing.id.to_s,
                      class: "form-check-input",
                      id: "listing_#{listing.id}" %>
                  <%= label_tag "listing_#{listing.id}", listing.title, class: "form-check-label", style: "font-size: 13px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 260px; display: block;" %>
                </div>
              <% end %>
              <div class="form-check mt-2 pt-2" style="border-top: 1px solid #F1F5F9;">
                <%= radio_button_tag :listing_id,
                    "",
                    params[:listing_id].blank?,
                    class: "form-check-input",
                    id: "listing_all" %>
                <%= label_tag "listing_all", "All listings", class: "form-check-label", style: "font-size: 13px; color: #64748B;" %>
              </div>
            </div>
          </div>
        <% end %>

        <%# CATEGORY — contractor only, shown if more than 1 specialty %>
        <% if current_user.contractor? && current_user.categories.count > 1 %>
          <div class="dropdown">
            <button class="btn custom-filter-btn <%= params[:category_ids].present? ? 'active' : '' %>"
                    type="button" data-bs-toggle="dropdown" data-bs-auto-close="outside">
              Category
              <% if params[:category_ids].present? %>
                <span class="badge rounded-pill ms-1" style="font-size: 10px;"><%= params[:category_ids].size %></span>
              <% end %>
              <i class="bi bi-chevron-down ms-1" style="font-size: 10px;"></i>
            </button>
            <div class="dropdown-menu p-3 shadow-sm" style="min-width: 200px; border-radius: 12px; border: 1px solid #E2E8F0;">
              <% current_user.categories.each do |category| %>
                <div class="form-check mb-2">
                  <%= check_box_tag "category_ids[]",
                      category.id,
                      Array(params[:category_ids]).map(&:to_s).include?(category.id.to_s),
                      class: "form-check-input",
                      id: "category_#{category.id}" %>
                  <%= label_tag "category_#{category.id}", category.name.titleize.gsub("Ac", "AC"), class: "form-check-label", style: "font-size: 13px;" %>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>

        <%# STATUS %>
        <div class="dropdown">
          <button class="btn custom-filter-btn <%= params[:status].present? ? 'active' : '' %>"
                  type="button" data-bs-toggle="dropdown" data-bs-auto-close="outside">
            Status
            <i class="bi bi-chevron-down ms-1" style="font-size: 10px;"></i>
          </button>
          <div class="dropdown-menu p-3 shadow-sm" style="min-width: 180px; border-radius: 12px; border: 1px solid #E2E8F0;">
            <div class="form-check mb-2">
              <%= radio_button_tag :status, "", params[:status].blank?, class: "form-check-input", id: "status_pending" %>
              <%= label_tag "status_pending", "Pending", class: "form-check-label", style: "font-size: 13px;" %>
            </div>
            <div class="form-check mb-2">
              <%= radio_button_tag :status, "accepted", params[:status] == "accepted", class: "form-check-input", id: "status_accepted" %>
              <%= label_tag "status_accepted", "Accepted", class: "form-check-label", style: "font-size: 13px;" %>
            </div>
            <div class="form-check">
              <%= radio_button_tag :status, "declined", params[:status] == "declined", class: "form-check-input", id: "status_declined" %>
              <%= label_tag "status_declined", "Declined", class: "form-check-label", style: "font-size: 13px;" %>
            </div>
          </div>
        </div>

        <%# SORT %>
        <div class="dropdown">
          <button class="btn custom-filter-btn <%= params[:sort_by].present? ? 'active' : '' %>"
                  type="button" data-bs-toggle="dropdown" data-bs-auto-close="outside">
            Sort by
            <i class="bi bi-chevron-down ms-1" style="font-size: 10px;"></i>
          </button>
          <div class="dropdown-menu p-3 shadow-sm" style="min-width: 200px; border-radius: 12px; border: 1px solid #E2E8F0;">
            <div class="form-check mb-2">
              <%= radio_button_tag :sort_by, "", params[:sort_by].blank?, class: "form-check-input", id: "sort_recent" %>
              <%= label_tag "sort_recent", "Most recent", class: "form-check-label", style: "font-size: 13px;" %>
            </div>
            <div class="form-check mb-2">
              <%= radio_button_tag :sort_by, "quote_asc", params[:sort_by] == "quote_asc", class: "form-check-input", id: "sort_quote_asc" %>
              <%= label_tag "sort_quote_asc", "Quote: low to high", class: "form-check-label", style: "font-size: 13px;" %>
            </div>
            <div class="form-check">
              <%= radio_button_tag :sort_by, "quote_desc", params[:sort_by] == "quote_desc", class: "form-check-input", id: "sort_quote_desc" %>
              <%= label_tag "sort_quote_desc", "Quote: high to low", class: "form-check-label", style: "font-size: 13px;" %>
            </div>
          </div>
        </div>

        <%# APPLY + CLEAR %>
        <%= f.submit "Apply",
            class: "btn btn-primary btn-sm px-4",
            style: "border-radius: 9999px; font-size: 13px;" %>
        <%= link_to "Clear", offers_path,
            class: "btn btn-link btn-sm",
            style: "font-size: 13px; color: #94A3B8; text-decoration: none;" %>
        <span class="text-muted" style="font-size: 12px;">
          <%= @offers.size %> offer<%= @offers.size == 1 ? "" : "s" %>
        </span>
      </div>
    <% end %>

    <%# OFFERS GRID %>
    <div class="row g-4">
      <% if @offers.empty? %>
        <div class="col-12">
          <p class="text-muted text-center py-5" style="font-size: 14px;">No offers found.</p>
        </div>
      <% else %>
        <% @offers.each do |offer| %>
          <%= render "shared/offer_card", offer: offer %>
        <% end %>
      <% end %>
    </div>

  </div>
</div>

<style>
  .custom-filter-btn {
    border-radius: 9999px !important; font-family: 'Outfit', sans-serif; font-size: 13px; padding: 4px 12px !important;
    background-color: #F8FAFC !important; color: #64748B !important; border: 1px solid #E2E8F0 !important; transition: all 0.2s ease;
    box-shadow: none !important; outline: none !important;
  }
  .custom-filter-btn:focus { box-shadow: none !important; outline: none !important; }
  .custom-filter-btn.active { background-color: #F8FAFC !important; color: #2F6BFF !important; border-color: #2F6BFF !important; }
  .custom-filter-btn .badge { background-color: #EFF6FF; color: #2F6BFF; }
</style>
